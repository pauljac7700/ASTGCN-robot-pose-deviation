# Direct dependencies for training and evaluation.
# Developed against Python 3.10.
torch>=2.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
joblib>=1.3
PyYAML>=6.0
matplotlib>=3.7
networkx>=3.0
tqdm>=4.65
tensorboard>=2.13
statsmodels>=0.14

# Only needed to re-run data acquisition on a physical UR5
# (see data/data_acquisition). Not required to train or evaluate.
# ur-rtde>=1.5
# roboticstoolbox-python>=1.1
# spatialmath-python>=1.1
